model TrueTraceApk {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  apk_name   String   @unique
  version    String   @unique
  file_path  String
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@map("true_trace_apks")
  @@schema("public")
}
